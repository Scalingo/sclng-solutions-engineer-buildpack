#!/bin/bash
#
# This script should be used as part of the multi-buildpack
# (https://doc.scalingo.com/platform/deployment/buildpacks/multi). The release
# script of the latest buildpack of the defined buildpacks will be used. End
# this buildpack should never be the last one.
#

BUILD_DIR=$1

cat <<EOF
---
EOF
